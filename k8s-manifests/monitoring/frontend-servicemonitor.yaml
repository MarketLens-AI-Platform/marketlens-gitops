apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: marketlens-frontend-monitor
  namespace: marketlens
  labels:
    release: marketlens-monitoring
spec:
  selector:
    matchLabels:
      app: frontend
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
